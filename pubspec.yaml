name: main_model
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  get: ^4.6.5

  permission_handler: ^11.3.1  # Updated to match agora_uikit dependency
  http: ^1.1.0
  uuid: ^4.0.0
  share: ^2.0.4
  zego_uikit: ^2.9.5
  zego_uikit_prebuilt_video_conference: ^2.9.5
  zego_express_engine: ^1.19.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
